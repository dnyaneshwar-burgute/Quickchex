<br/>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_contact_list_path,
            :class => 'btn btn-primary' %></br></br>
  <div id="search_result">
    <%= render partial: 'contacts', locals: {contact_lists: @contact_lists} %>
  </div>
  <div id='search_div'>
    <%= render 'search_form' %>
  </div>
<div>